#lang rhombus

import:
  racket/base
  open:
    "../class.rkt"
    "decl.rkt"

val a: new Add(~left: 3, ~right: 4)
val b: new Sub(~left: 32, ~right: 30)

base.exit(if a.eval() == 7 && b.eval() == 2
          | 0
          | base.displayln("oops")
            1)

